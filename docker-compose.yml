services:
  a:
    build: a
    container_name: web-a
    expose:
      - "8000"
    environment:
      - VIRTUAL_HOST=a.noboroto.id.vn
    restart: unless-stopped

  calibre-web:
    image: lscr.io/linuxserver/calibre:arm64v8-latest
    container_name: calibre-deployed
    security_opt:
      - seccomp:unconfined #optional
    environment:
      - PUID=1000
      - PGID=1000
      - TZ=Asia/Ho_Chi_Minh
      - PASSWORD= #optional
      - CLI_ARGS= #optional
      - VIRTUAL_HOST=calibre.noboroto.id.vn
    volumes:
      - /home/david0403/Github/calibre-server/config:/config
    expose:
      - "8080"
    restart: unless-stopped

  nginx-proxy:
    image: nginxproxy/nginx-proxy
    depends_on: 
      - calibre-web
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - /var/run/docker.sock:/tmp/docker.sock:ro
    restart: always
    privileged: true